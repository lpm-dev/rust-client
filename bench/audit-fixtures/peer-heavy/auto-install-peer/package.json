{
	"name": "audit-fixture-auto-install-peer",
	"version": "1.0.0",
	"private": true,
	"_comment": "Eager peer auto-install fixture. Declares react-redux only; react (a required peer of react-redux@^9) is intentionally absent from dependencies. The resolver's peer-drain pass detects the unmet required peer and synthesizes a root-scoped ambient install, so react lands at node_modules/react/ and react-redux loads cleanly.",
	"dependencies": {
		"react-redux": "^9.0.0"
	}
}
