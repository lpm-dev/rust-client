# @lpm-registry/cli-linux-x64

Platform-specific binary for `@lpm-registry/cli` on Linux x64.

This package is installed automatically as an `optionalDependency` of `@lpm-registry/cli`. You should not install it directly.

See https://lpm.dev for more information.
