# @lpm-registry/cli-linux-x64-musl

Native Linux x64 musl binaries for `@lpm-registry/cli`.

Install the wrapper package instead of depending on this package directly:

```sh
npm install -g @lpm-registry/cli
```
